import React, { FC } from "react";
import { NavbarContainer } from "./styled-navbar";

export const Navbar: FC = () => {
  return (
    <NavbarContainer>
      <p>Navbar</p>
    </NavbarContainer>
  )
}